{
  "name": "persistent-pr",
  "version": "1.0.0",
  "description": "Ensure that if a PR can be open between two branches, that it is.",
  "main": "index.js",
  "scripts": {
    "start": "node ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "4.4.4"
  },
  "author": "Ollie Buck <ollie.buck@holidayextras.com>",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.1",
    "express": "^4.13.4",
    "github": "^0.2.4"
  }
}
